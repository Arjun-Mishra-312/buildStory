# Local package artifacts

Scanner packaging moved to the public [`buildstory-scan`](https://github.com/Arjun-Mishra-312/buildstory-scan) repository. Archives in this folder are historical local tarballs and are no longer produced from this workspace.

```powershell
npm install --global buildstory-scan
buildstory-scan --version
```

The npm package `buildstory-scan` and the binary of the same name are published from that repo. `buildstory` and `npx buildstory` are unrelated packages.
